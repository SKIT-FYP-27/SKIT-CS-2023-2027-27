PORT=5000

# PostgreSQL connection (matches Database/schema.sql)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/uei_platform

# JWT
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d
