PORT=5000

# PostgreSQL connection (matches Database/schema_v2.sql, accessed via Prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/uei_platform

# JWT
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d
